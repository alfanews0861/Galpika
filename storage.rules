rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Anyone can read news thumbnails and images
    match /news/{allImages=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
